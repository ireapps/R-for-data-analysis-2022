---
title: "Summarizing"
output: html_notebook
---

As a reminder, always add your packages at the top of your script

```{r setup}
library(tidyverse)
```

For this exercise we'll be working with the poverty data and MLB salaries data again.

```{r}
poverty <- read_csv("data/poverty.csv")
mlb <- read_csv("data/mlb.csv")
```

### Summarizing

Q: What is the total payout for mlb salaries in 2021?

```{r}
mlb %>% summarise(total_pay = sum(salary))
```
Q: What are the average and median salaries in mlb? 
```{r}

```

Q: How do these national numbers compare to those of your favorite team? 
```{r}

```

